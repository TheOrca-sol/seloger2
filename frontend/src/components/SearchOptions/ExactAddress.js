import React from 'react';
import { Checkbox } from 'antd';

const ExactAddress = () => (
  <div>
    <h3>Emplacement</h3>
    <Checkbox value="exactAddress">Adresse exacte uniquement</Checkbox>
  </div>
);

export default ExactAddress;
